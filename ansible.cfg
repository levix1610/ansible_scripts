[ssh_connection]
# Keep pipelining on for speed
pipelining = false
retries = 3

# Explicitly disable ControlMaster to stop container socket corruption
ssh_args = -o ControlMaster=no -o ServerAliveInterval=15 -o ServerAliveCountMax=5 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null